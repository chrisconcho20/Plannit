# Local Supabase development only (supabase start). These are secrets:
# copy to supabase/.env, which is git-ignored, and never commit real values.
APPLE_CLIENT_ID=
APPLE_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_SECRET=
