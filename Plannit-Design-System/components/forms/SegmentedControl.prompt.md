Switch between views of the same content. Two to four segments; the thumb slides with the iOS easing.

```jsx
<SegmentedControl options={['Month','Week','List']} value={view} onChange={setView} />
```

Plannit has no underlined tab bar — this is the tab pattern.
