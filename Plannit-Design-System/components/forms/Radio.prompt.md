Exclusive choice in a settings or constraint list.

```jsx
<Radio checked={mode==="vote"} onChange={()=>setMode("vote")} label="Propose then vote" sublabel="The group picks from the top three slots" />
```
