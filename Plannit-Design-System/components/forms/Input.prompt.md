Text entry — event titles, group names, notes. Constraints are tapped, not typed (see `DayOfWeekPicker`).

```jsx
<Input label="Event title" icon="calendar-plus" placeholder="Five-a-side at the pitch" />
<Input label="Name this plan" placeholder="Five-a-side" icon="pencil" />
```

Placeholders are lowercase real examples, never "Enter title…".
