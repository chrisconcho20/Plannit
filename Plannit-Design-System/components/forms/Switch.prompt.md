Instant on/off settings — sync, notifications, per-group sharing. Pair with ListRow.

```jsx
<ListRow icon="repeat" title="Two-way sync" right={<Switch checked onChange={setOn} />} />
```

Never use a switch for something that needs a Save button.
