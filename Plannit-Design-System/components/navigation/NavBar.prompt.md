Top chrome for every screen — translucent paper with an 18px blur so content scrolls under it.

```jsx
<NavBar large title="August" subtitle="4 plans this week" actions={[{name:'search',label:'Search'},{name:'plus',label:'New'}]} />
<NavBar back title="Five-a-side" onBack={goBack} />
```
