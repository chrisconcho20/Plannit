Explains an icon-only control where hover exists.

```jsx
<Tooltip label="Only Soccer can see this"><Icon name="lock" /></Tooltip>
```

On touch screens use a `sublabel` or footnote instead — never a tooltip.
