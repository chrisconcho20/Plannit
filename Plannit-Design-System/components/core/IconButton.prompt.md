Round icon-only button for nav bars, card overflow, and the compose FAB.

```jsx
<IconButton name="chevron-left" label="Back" />
<IconButton name="plus" variant="primary" size={56} label="New event" />
```

Use `chrome` when it floats over a photo or calendar grid. Always pass `label`.
