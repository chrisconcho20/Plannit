The container for everything in a Plannit list.

```jsx
<Card elevation={1} accent="var(--hue-teal)">…</Card>
```

`accent` is only for events that belong to a group — the spine carries that group's hue. Interactive cards get a 0.988 press scale.
