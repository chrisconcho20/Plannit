# Environment / secrets
.env
.env.*
!.env.example
# Keys and credentials: APNs and Sign in with Apple keys (AuthKey_*.p8),
# certificates, and service-account JSON never belong in this public repo.
*.p8
*.p12
*.pem
*.key
*.cer
*.mobileprovision
*credentials*.json
*service-account*.json
GoogleService-Info.plist

# Supabase local
supabase/.branches
supabase/.temp
supabase/.env

# Deno
deno.lock

# Node tooling (if introduced)
node_modules/

# Claude Code / agent-skill install artifacts (local tooling, not project code)
.claude/
.agents/
skills-lock.json
package.json
package-lock.json

# Xcode / iOS build artifacts
ios/Plannit.xcodeproj/
ios/build/
ios/DerivedData/
**/xcuserdata/
*.xcuserstate

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
